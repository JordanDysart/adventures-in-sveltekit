import { dev } from '$app/environment';

export const title = 'Build with Me';
export const description = 'A blog dedicated to documenting the steps of building itself';
export const url = dev ? 'http:/localhost:5173/' : 'https://buildwithme.netlify.app/';
